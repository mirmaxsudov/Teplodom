import { createSlice } from "@reduxjs/toolkit";

const favouriteSlice = createSlice({
  name: "category",
  initialState: {},
  reducers: {},
});

// export const categoryActions = favouriteSlice.actions;
export default favouriteSlice.reducer;
